# Graphite AI Layer — Advisory Intent Parser
#
# The intent parser uses only Python stdlib (json, re, argparse, http.server).
# No external runtime dependencies are required for basic operation.
#
# These dependencies are for testing and optional enhanced parsing:

# Testing. Pinned exactly: a floating lower bound resolves to whatever is
# newest when CI runs, so the build of a security tool would depend on a
# package version nobody in this repository chose.
pytest==8.3.4

# Optional: If you integrate LLM-based intent parsing (Constitution P1 still applies —
# the AI layer only PARSES, the Rust Core makes all security decisions):
# openai>=1.0.0
# pydantic>=2.0.0
